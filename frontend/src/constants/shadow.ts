const baseBoxShadow = "0px 20px 25px -5px rgba(0, 0, 0, 0.07)";
const sideMenuShadow = 'inset 8px 0px 20px -8px rgba(0, 0, 0, 0.2)';
const tableShadow = "0px 0px 10px 5px rgba(0, 0, 0, 0.05)";
const headerShadow = "0px 4px 4px rgba(0, 0, 0, 0.55)";

const shadows = {
  baseBoxShadow,
  tableShadow,
  headerShadow,
  sideMenuShadow
};

export default shadows;
